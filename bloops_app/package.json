{
  "name": "bloops_app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check",
    "postinstall": "node scripts/apply-patches.cjs",
    "test": "echo 'Vitest disabled in this environment'",
    "test:run": "npm run test",
    "coverage": "npm run test"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.0",
    "svelte-check": "^3.6.0",
    "vite": "^4.5.0"
  }
}
